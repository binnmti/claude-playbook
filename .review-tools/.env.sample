# commit/push レビューフックのマスタースイッチ。
# このファイルを .env にコピーした環境でだけレビューが発動する。
#   cp .env.sample .env
# .env が無い環境では git-hooks が繋がっていても何もしない (exit 0)。
REVIEW_ENABLED=1

# vllm reviewer credential (models.conf sets VLLM_BASE_URL, but the key stays
# out of tracked files)
VLLM_API_KEY="xxxxx"
